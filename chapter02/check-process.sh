#!/bin/bash

# check out the `process.versions` property
node -p "process.versions"

# check out the `process.env` property
node -p "process.env"

# check out the `process.release` property
node -p "process.release"

# check out the `process.release.lts` property
node -p "process.release.lts"
